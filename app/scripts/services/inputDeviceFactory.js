'use strict';

angular.module('angularificationApp')
  .service('Inputdevicefactory', function Inputdevicefactory() {
    // AngularJS will instantiate a singleton by calling "new" on this function
  });
